# Arquivo de exemplo para variáveis de ambiente
# Copie este arquivo para .env e configure as variáveis conforme necessário

# Azure Key Vault (obrigatório para secrets em produção)
AZURE_KEYVAULT_URI=https://your-keyvault.vault.azure.net/
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret  
AZURE_TENANT_ID=your-azure-tenant-id

# Database (apenas para desenvolvimento local com Docker)
# Em produção, a connection string será obtida do Key Vault
DB_SA_PASSWORD=YourStrong!Passw0rd

# Configurações não sensíveis
ASPNETCORE_ENVIRONMENT=Docker

# Redis (para desenvolvimento local)
REDIS_CONNECTION=redis:6379

# Notas importantes:
# - Todas as chaves, senhas e connection strings devem estar no Azure Key Vault
# - As seguintes secrets devem ser configuradas no Key Vault:
#   - ConnectionStrings--DefaultConnection (connection string do banco de produção)
#   - AzureServiceBus--ConnectionString
#   - EventGrid--TopicEndpoint  
#   - EventGrid--AccessKey
#   - PaymentGateway--ApiKey
#   - PaymentGateway--BaseUrl
#   - Jwt--SecretKey
# - Use managed identity sempre que possível para autenticação no Azure